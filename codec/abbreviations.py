"""
-----------------------------------------------------------------------
abbreviations.py -- Defines numpy abbreviations

Author: Wisam Reid
-----------------------------------------------------------------------
"""

import numpy as np
import matplotlib.pyplot as plt

# numpy abbreviations

# constants
pi = np.pi
inf = np.inf

# arrays
array = np.array
arange = np.arange
zeros = np.zeros
ones = np.ones
ones_like = np.ones_like
append = np.append
array_str = np.array_str
concatenate = np.concatenate
zeros_like = np.zeros_like
empty = np.empty
size = np.size
vstack = np.vstack
argwhere = np.argwhere

# types
float64 = np.float64
uint16 = np.uint16
int64 = np.int64

# plotting
linspace = np.linspace
subplot = plt.subplot
plot = plt.plot
ylim = plt.ylim
xlim = plt.xlim
xlabel = plt.xlabel
ylabel = plt.ylabel
title = plt.title
grid = plt.grid
legend = plt.legend
show = plt.show
figure = plt.figure
semilogx = plt.semilogx
axes = plt.axes
vlines = plt.vlines
text = plt.text

# transforms
fft = np.fft.fft
ifft = np.fft.ifft
fftfreq = np.fft.fftfreq

# math
cos = np.cos
sin = np.sin
sum = np.sum
mean = np.mean
sqrt = np.sqrt
multiply = np.multiply
exp = np.exp
add = np.add
real = np.real
random = np.random
cumsum = np.cumsum
i0 = np.i0 # modified Bessell
log10 = np.log10
maximum = np.maximum
clip = np.clip
power = np.power
arctan = np.arctan
divide = np.divide
subtract = np.subtract
absolute = np.absolute
greater_equal = np.greater_equal
greater = np.greater
argmax = np.argmax
less = np.less
diff = np.diff
amax = np.amax


